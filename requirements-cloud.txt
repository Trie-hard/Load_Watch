# Minimal deps for Streamlit Community Cloud (dashboard only).
# Rename to requirements.txt for deploy, or copy these lines into the app settings.
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0
streamlit>=1.31.0
plotly>=5.18.0
