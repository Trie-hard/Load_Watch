# Dashboard + pre-scored demo on Streamlit Community Cloud (default install file).
# For local pipeline/training, use: pip install -r requirements-pipeline.txt
pandas>=2.1.0,<3
numpy>=1.26.0,<3
pyarrow>=14.0.0
streamlit>=1.31.0
plotly>=5.18.0
